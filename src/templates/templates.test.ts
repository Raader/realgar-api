import { describe, it } from "mocha";
import chai from "chai";
import chaiAsPromised from "chai-as-promised";
import { getTemplatesFromCollection } from ".";

chai.use(chaiAsPromised);
const expect = chai.expect;

describe("templates", () => {});
